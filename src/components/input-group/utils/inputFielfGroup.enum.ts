export enum EInputFieldGroupType {
  TEXT = 'text',
  PASSWORD = 'password',
  EMAIL = 'email',
  NUMBER = 'number',
  TEL = 'tel',
  URL = 'url',
  SEARCH = 'search',
  FILE = 'file',
  HIDDEN = 'hidden',
}

export enum EStateInputFieldGroup {
  DEFAULT = 'default',
  ERROR = 'error',
  SUCCESS = 'success',
}

export enum EAutoCapitalizeOptions {
  OFF = 'off',
  NONE = 'none',
  ON = 'on',
  SENTENCES = 'sentences',
  WORDS = 'words',
  CHARACTERS = 'characters',
}

export enum EAutoCorrectOptions {
  ON = 'on',
  OFF = 'off',
}
